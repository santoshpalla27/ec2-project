# Application settings
PORT=3000
ENVIRONMENT=${ENVIRONMENT}
APP_VERSION=${APP_VERSION}
NODE_ENV=production

# Database settings
DB_HOST=${DB_HOST}
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=admin123
DB_NAME=project

# Redis cache settings (cluster mode)
REDIS_NODES=${REDIS_NODES}
REDIS_PASSWORD=santosh
REDIS_TLS=false

# Logging settings
LOG_LEVEL=info